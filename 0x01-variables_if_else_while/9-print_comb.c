#include <stdlib.h>
#include <string.h>
#include <stdio.h>

/**
 * main - start function
 * Return: return 0
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
		if (i != 57)
		{
			putchar(',');
			putchar(' ');													}
	}
	putchar('\n');
	return (0);

}
