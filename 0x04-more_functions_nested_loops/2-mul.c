#include<stdio.h>

/**
 * mul- multiply two numbers
 *
 * @a : number to be multiplied to b
 * @b : number to be multiplied to a
 *
 * Return: returns the ans
 */
int mul(int a, int b)
{
	return (a * b);
}
